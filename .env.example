# Supabase Configuration
# IMPORTANT: Never commit your actual .env file to version control
# The anon key is safe to use in client-side code, but ensure RLS is enabled in Supabase

# Your Supabase project URL (must be HTTPS)
VITE_SUPABASE_URL=YOUR_SUPABASE_URL

# Your Supabase anonymous/public key (client-safe)
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Security Notes:
# 1. Always use HTTPS URLs
# 2. Enable Row Level Security (RLS) in your Supabase database
# 3. Set up proper policies for the 'subscribers' table
# 4. Never expose service_role keys in client code
# 5. Keep your .env file in .gitignore
